taf.constants <-
  list(
    boot = "boot",
    boot.data = "data",
    boot.library = "library",
    boot.software = "software",
    taf.sections = list(
      data = "data",
      method = "method",
      output = "output",
      report = "report"
    )
  )